# Backend

Express API + Socket.IO server for Gapistan.

See the **[root README](../README.md)** for full setup, Docker, and environment variables.

```bash
cp .env.example .env
npm install
npm run dev
```

API runs at http://localhost:3000.
