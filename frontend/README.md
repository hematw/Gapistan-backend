# Frontend

React + Vite client for Gapistan.

Documentation, setup, and Docker instructions live in the **[root README](../README.md)**.

```bash
cp .env.example .env
npm install
npm run dev
```

Runs at http://localhost:5173 by default.
